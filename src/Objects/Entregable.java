package Objects;

public interface Entregable {
	
	// Cambia el valor del campo entregado del objeto a true
	public void entregar();
	// Cambia el valor del campo entrgado del objeto a false
	public void devolver();
	// Devuelve el valor de campo entregado del objeto
	public boolean isEntregado();
	
}
